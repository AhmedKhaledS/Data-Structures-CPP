#ifndef UTILITY_H
#define UTILITY_H


class Utility
{
    public:
        /// Generates the next prime number just after the given number.
        static long long int generateNextPrime(long long int maximumKey, int spaceSize);

    protected:

    private:
        Utility();
};

#endif // UTILITY_H
